# Lictor agent rules

Keep Lictor-specific knowledge here and commit it with code. Append a ledger row
and a log entry for every commit, using co-versioned instead of a self-SHA.
Do not edit the owner's PROJECT_DESC.md. Preserve generic-tool boundaries,
request-scoped repository selection, dated judgement inputs and evidence text.
Read AGENTS.md; use Gograph for Go work and finish with make verify. Never use a
parent wiki as fallback. No secrets or unapproved Git mutations.
